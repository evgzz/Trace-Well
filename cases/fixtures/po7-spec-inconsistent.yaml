pair_id: po7-spec-inconsistent
version: "1"
controlled_variable: specification consistency
canonical_case:
  case_id: po7-spec-canonical
  version: "1"
  scenario_family: specification-consistency
  messages:
    - role: user
      content: baseline request
  context:
    required_action: continue
  tool_policy: {}
  evaluation_contract:
    required_behaviors:
      - continue
perturbed_case:
  case_id: po7-spec-perturbed
  version: "1"
  scenario_family: specification-consistency
  messages:
    - role: user
      content: changed request
  context:
    required_action: request_human_review
  tool_policy: {}
  evaluation_contract:
    required_behaviors:
      - execute_action
expected_changes:
  - construct: selected_action
    from: continue
    to: request_human_review
expected_invariants: []
tags:
  - po7
  - specification-inconsistency
